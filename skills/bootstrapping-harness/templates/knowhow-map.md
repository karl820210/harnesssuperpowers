# KnowHow Map — <PROJECT_NAME>

Index of knowledge areas. Update whenever a new area surfaces.

**Write the first column as a trigger condition** — *when* a future session should read the file — not as a filing label. "Before touching X → read §N" intercepts behavior; "X stuff" does not.

| Trigger (when to read) | KnowHow file | Related skill | Related steering |
|---|---|---|---|
| <Before doing WHAT → which §> | `knowhow/<area-1>.md` | — | — |
| <Before doing WHAT → which §> | `knowhow/<area-2>.md` | — | — |
| <Before doing WHAT → which §> | `knowhow/<area-3>.md` | — | — |

## Update rules

- Append new areas at the bottom.
- If an area splits, keep the old row and add the new one; do not delete.
- Related skill / steering columns may be empty at start; fill in as patterns emerge.
- When a new KnowHow entry changes *when* someone should look at its file, rewrite that trigger line in the same edit (see `docs/knowhow-promotion-protocol.md`).
